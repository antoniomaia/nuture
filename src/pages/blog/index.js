import React from 'react';
import styles from './styles.module.scss';

const Blog = () => {
  return (
    <section className={styles.root}>
      <h1>Ecological Earth Blog</h1>
    </section>
  );
};

export default Blog;
