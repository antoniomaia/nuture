import React from "react";
import Logo from "../logo";
import "./styles.css";

const Header = () => {
  return (
    <header className="App-header">
      <Logo />
    </header>
  );
};

export default Header;
